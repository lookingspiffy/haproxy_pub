Some generalized config files handy for an HAProxy pair. Most specific details (VIP, auth details, and DNS entries) are generalized and would need to be replaced by details specific to your environment.
